// src/components/CreateGame.js
import React from 'react';

const CreateGame = () => (
  <div>
    <h2>Create a New Game</h2>
    {/* TODO: Add game creation form (mode selection, public/private toggle, etc.) */}
  </div>
);

export default CreateGame;