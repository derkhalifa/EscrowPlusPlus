// src/components/GameBoard.js
import React from 'react';

const GameBoard = () => (
  <div>
    <h2>Game Board</h2>
    {/* TODO: Render the game interface (card decks, player turns, actions, etc.) */}
  </div>
);

export default GameBoard;